# Local Fish4Knowledge data

Data are manually downloaded and must not be committed. Set `data_root` in the YAML configuration. Code references original files in place and produces online augmentation; it never copies the full dataset or materialises augmented images.
