# Local split manifests

Gate-0 writes deterministic image-level, trajectory-level, and locked outer-fold manifests
here after auditing a locally downloaded dataset. They include machine-local source paths and
are therefore intentionally not committed. The split builder records their SHA-256 hashes in
the local manifest for provenance.
